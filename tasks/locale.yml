---

- ansible.builtin.include_tasks: configure.deb.yml
  when: ansible_facts['os_family'] == 'Debian'

- ansible.builtin.include_tasks: configure.yum.yml
  when: ansible_facts['os_family'] == 'RedHat'
